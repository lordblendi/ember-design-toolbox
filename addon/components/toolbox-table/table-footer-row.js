import TableRowComponent from "./table-row";
import layout from "../../templates/components/toolbox-table/table-footer-row";

// A specialized version of a row component, where the content is marked as being a footer
export default TableRowComponent.extend({
  layout
});
