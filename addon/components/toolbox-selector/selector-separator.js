import Component from "@ember/component";
import layout from "../../templates/components/toolbox-selector/selector-separator";

export default Component.extend({
  layout,
  tagName: "div",
  classNames: ["selector__itemSeperator"]
});
